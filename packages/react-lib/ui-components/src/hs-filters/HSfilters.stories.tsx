import React, { FC } from 'react';
import { HsFilters } from './HSfilters';
export default {
  title: 'HsFilters',
  component: HsFilters,
};

export const HsFiltersComponent: FC = () => (
    <HsFilters  />
  );
  